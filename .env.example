# Used by crawler scripts (quickstart.js, crawl.js) to pre-bake Claude insights into math_graph.json
ANTHROPIC_API_KEY=your_key_here

# Used only for local dev fallback when math_graph.json has no pre-baked insights
VITE_ANTHROPIC_API_KEY=your_key_here
